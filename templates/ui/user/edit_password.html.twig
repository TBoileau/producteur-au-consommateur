{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="row justify-content-center">
        <div class="col-6">
            <div class="card">
                <div class="card-header">
                    <h1 class="mb-0">Modifier mon mot de passe</h1>
                </div>
                <div class="card-body">
                    {{ form_rest(form) }}
                </div>
                <div class="card-footer">
                    <button class="btn btn-block btn-primary">Modifier</button>
                </div>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
